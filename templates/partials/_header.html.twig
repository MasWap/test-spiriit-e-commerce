<header class="bg-dark text-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="{{ path('app_accueil') }}">
                <i class="fas fa-guitar me-2"></i>
                <span class="fw-bold">LAYRAC Guitar Shop</span>
            </a>
            
            <!-- Ajout du bouton de navigation pour les petits écrans -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_accueil') }}">
                            <i class="fas fa-guitar me-1"></i>{{ 'nav.collection'|trans }}
                        </a>
                    </li>
                    <li class="nav-item">
                        <!-- Lien vers le panier & affichage dynamique du nombre d'articles -->
                        <a class="nav-link" href="{{ path('app_panier') }}">
                            <i class="fas fa-shopping-cart me-1"></i>{{ 'nav.cart'|trans }}
                            <span class="badge bg-primary ms-1">{{ panier_count() }}</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin') }}">
                            <i class="fas fa-cog me-1"></i>Administration
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="languageDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="fas fa-globe me-1"></i>Langue
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="languageDropdown" style="z-index: 9999;">
                            <li>
                                <a class="dropdown-item" href="{{ path('app_change_language', {locale: 'fr'}) }}">
                                    Français 🇫🇷
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('app_change_language', {locale: 'en'}) }}">
                                    English 🇬🇧
                                </a>
                            </li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>
